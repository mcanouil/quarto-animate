name: Deploy GitHub Pages

on:
  release:
    types: [published]

jobs:
  deploy:
    uses: mcanouil/quarto-extensions-actions/.github/workflows/deploy.yml@main
    with:
      formats: "html"
      tinytex: false
      version: "release"
